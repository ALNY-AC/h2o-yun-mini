<view class="store">
    <view class="store-header">
        <van-card custom-class='good-card' title="超牛送水" thumb="{{ imageURL }}" />
    </view>
    <view class="store-set">
        <van-grid column-num="3">
            <van-grid-item icon="photo-o" text="{{item.text}}" wx:for="{{ list }}" wx:key="index"/>
        </van-grid>
    </view>
</view>